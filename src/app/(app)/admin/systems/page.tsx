export default function AdminSystemsPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">システム管理（仮）</h1>
      <p className="mt-2 text-slate-600 text-sm">
        systemsの追加・編集・無効化をここに実装します（次タスクH）。
      </p>
    </div>
  );
}
